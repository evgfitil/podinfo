apiVersion: v1
kind: ConfigMap
metadata:
  name: podinfo-redis
  labels:
    app: podinfo-redis
data:
  redis.conf: |
    maxmemory 64mb
    maxmemory-policy allkeys-lru
    save ""
    appendonly no
